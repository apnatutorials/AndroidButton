<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center|top"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.apnatutorials.androidbutton.MainActivity">

    <Button
        android:id="@+id/btnBorderLess"
        style="?android:attr/borderlessButtonStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:onClick="handleBorderLessButtonClick"
        android:text="BorderLess button"
        android:theme="@style/MyButton" />


    <Button
        android:id="@+id/btnTextAndImage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/btnBorderLess"
        android:layout_marginBottom="10dp"
        android:drawableLeft="@android:drawable/ic_lock_idle_lock"
        android:text="Text &amp; Image"
        android:theme="@style/MyButton" />

    <Button
        android:id="@+id/btnButtonSelector"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/btnTextAndImage"
        android:layout_marginBottom="10dp"
        android:background="@drawable/button_selector"
        android:text="Click me"
        android:theme="@style/MyButton" />

    <Button
        android:id="@+id/btnSimpleButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/btnButtonSelector"
        android:text="Simple button" />

</RelativeLayout>
